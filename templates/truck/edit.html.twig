{% extends 'base.html.twig' %}

{% block title %}Edit Truck!{% endblock %}

{% block body %}
    <h1>Edit Truck! ✅</h1>

    <form action="{{path('truck_update', {id: truck.id})}}" method="POST">
        Maker:<input type="text" name="truck_maker" value="{{truck.maker}}">
        Plate:<input type="text" name="truck_plate" value="{{truck.plate}}">

        Make year:<input type="text" name="truck_make_year" value="{{truck.makeYear}}">
        Machanic notices:<textarea name="truck_mechanic_notices">{{truck.mechanicNotices}}</textarea>
        <select name="truck_mechanic_id">
        {% for mechanic in mechanics %}
        <option value="{{mechanic.id}}" {% if mechanic.id == truck.mechanicId %} selected {% endif %}>
        {{mechanic.name}} {{mechanic.surname}}</option>
        {% endfor %}
        </select>
        <button type="submit">Edit Truck</button>
    </form>

</div>
{% endblock %}